#include "JobQueue.h"
#include <list>
#include <thread>
/*
    JobQueue::JobQueue()
    {
        _maxThreads = 10; // figure out the max system threads?
        _activeThreads = 0;

        // spawn a thread which calls Process function

    }

    JobQueue::~JobQueue()
    {
        // ensure all threads are dead/process function thread is dead

        //dtor
    }

void Process(){
    // while jobqueue is not empty, process items


}

void ThreadFinished(int threadId){
    // mark thread as finished so it can grab the next item in the queue
}


void *PrintHello(void *threadarg) {
}

void JobQueue::QueueJob(callback_function f, std::list<int> entityIds){


    for (std::list<int>::iterator entityId = entityIds.begin(); entityId != entityIds.end(); ++entityId){
    //    pthread_create(nullptr,nullptr,PrintHello, (void *)*entityId);


    }
}
*/
