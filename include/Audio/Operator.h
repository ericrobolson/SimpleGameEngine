#ifndef OPERATOR_H
#define OPERATOR_H


class Operator
{
    public:

/*
        // https://en.wikipedia.org/wiki/Frequency_modulation_synthesis
//https://wiki.libsdl.org/Tutorials/AudioStream
        Operator(){
//https://davidgow.net/handmadepenguin/ch8.html


        };

        virtual ~Operator();


        void In(Operator op); //
        void In(float frequency);// takes the midi note

        float GetValue();

        float GetFrequency();

        int SetRatio(float ratio);



    protected:

    private:
        int _currentValue;

        static const int _lookupResolution = 512; //


        float _sineLookupTable[_lookupResolution];


        // todo: need to research what types we want to use
        static const float _samplingFrequencyHz = 40000; // nyquist sampling theorem

        static const float _maxHz = _samplingFrequency / 2; // todo: whenever calculating values above; truncate them?
        static const float _minValue = 0;
*/
};

#endif // OPERATOR_H
